# Legacy / non-standard label → canonical mapping.
# Used by label-cleanup.yml. `null` means: do not auto-migrate; flag in report.

aliases:
  # GitHub defaults
  bug:                kind/bug
  documentation:      kind/docs
  duplicate:          resolution/duplicate
  enhancement:        kind/enhancement
  "good first issue": good-first-issue
  "help wanted":      help-wanted
  invalid:            resolution/invalid
  wontfix:            resolution/wontfix
  question:           kind/rfc
  # Common drift seen in third-party templates
  feature:            kind/feature
  feature-request:    kind/feature
  bugfix:             kind/bug
  ci:                 area/ci
  infra:              area/infra
  docs:               kind/docs
  # Dependabot / GitHub Actions auto-labels
  dependencies:       kind/chore
  github_actions:     area/ci
  terraform:          area/infra
  # Legacy agile labels (pre-agile/* namespace)
  kind/epic:          agile/epic
  kind/story:         agile/story
  kind/task:          agile/task
