name: Self-install labels (dogfood)

on:
  workflow_dispatch:

jobs:
  install:
    uses: convergent-systems-co/repo-standards/.github/workflows/label-install.yml@v1.0.0-dogfood
    with:
      labels-ref: main
      delete-others: false
